# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
Package.resolved
# …but keep the app's resolved pin so SPM installs are reproducible.
!ball_tracker.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
.build/

# CocoaPods
Pods/

# Carthage
Carthage/Build/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/

# Env
.env
.env.local

# Server runtime state (pitch/results cache, written per-request).
server/data/

# Tools venv (SAM 3 + transformers main, ~5 GB; managed by uv from tools/pyproject.toml)
tools/.venv/
tools/.python-version

# Per-device calibration artefacts (large image sets + device-specific JSON).
server/calib_A/
server/calib_B/
server/intrinsics_*.json
server/charuco_board.png

# Claude Code session scratch — except checked-in skills
.claude/*
!.claude/skills/
!.claude/skills/**

.DS_Store
