  // Three.js scene runtime owns view-preset toolbar binding (called
  // from `setupViewerLayers` after mount). The legacy
  // `window.BallTrackerViewPresets` Plotly.relayout binder is gone
  // along with its server-side runtime in phase 4 cleanup.
